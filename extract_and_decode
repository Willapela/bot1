def extract_and_decode(raw: str) -> tuple[str | None, str, str]:
    # 1) javascript-obfuscator (prioridade)
    if is_js_obfuscator(raw):
        try:
            result = deobfuscate_js_obfuscator(raw)
            if result and len(result.strip()) > 100:
                return result, "javascript-obfuscator", "js"
        except Exception as e:
            # continua para os outros métodos
            pass

    # ... resto do código (phpkobo, unescape, atob etc.)
